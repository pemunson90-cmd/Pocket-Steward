# Signing/build instructions

The original file is retained in the separate private signing-input ZIP because it accompanies release credentials. See `brief/BUILD_AND_RELEASE.md` from the package root for the current build instructions and environment variable names. Do not generate a replacement release key.
