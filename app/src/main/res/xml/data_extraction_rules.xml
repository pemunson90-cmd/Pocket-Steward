<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ ignores android:allowBackup="false" for device-to-device
    transfer. The app already opts out of backup; these rules make that intent
    hold on newer Android too. The file index, the undo journal and the derived
    content-search database stay on this phone. User files are never app data
    and are unaffected.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
