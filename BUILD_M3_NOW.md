# Build M3 Now

Build the current tree exactly as supplied.

```bash
./gradlew clean assembleDebug testDebugUnitTest --stacktrace
```

If compilation or tests fail, fix only what is necessary to make Milestone 3 green. Do not redesign the architecture or advance to Milestone 4. Return the resulting debug APK and a short list of fixes.
